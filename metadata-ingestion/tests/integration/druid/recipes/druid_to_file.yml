source:
  type: druid
  config:
    host_port: "localhost:8082"
    platform_instance: "my_druid"
    bearer_token: "test-bearer-token-123"
sink:
  type: file
  config:
    filename: "./druid_mces.json"
